name: Tests
on:
  pull_request: null
  push:
    branches:
      - main
      - devel
jobs:
  linux_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.8"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e '.[dev,test]'

      - name: Bin check
        run: |
          cat "$(which blob_descriptor)" || true
          echo "----------------"
          blob_descriptor --help

      - name: Run tests
        run: sh tests/run_tests_with_coverage.sh
      - name: Run mypy
        run: mypy || true
      - name: Run flake8
        run: flake8 || true
